<% provide(:title, @title) %>
<div class="row">
  <aside class="span4 fixed_left">
    <section> 
      <%= carrier_for @user %>
      <h1><%= @user.name %></h1>
      <span><%= link_to "view  profile", @user %></span>
      <span><b>Microposts:</b> <%= @user.microposts.count %></span>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="span8 fixed_right">
    <h3><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>
</div>